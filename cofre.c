#include <stdio.h>

#define MAX_PILHA 100

typedef struct Fila{
    int centA[MAX_PILHA];
    int centB[MAX_PILHA];
    int teste = 1;
}Fila;

int main() {
    int N;
    while(scanf("%d", &N) = 0){
        
    }
    return 0;
}
